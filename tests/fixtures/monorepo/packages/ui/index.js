// A file, not just a directory: an empty directory does not survive a tarball, and a
// fixture that changes shape when published tests something other than what ships.
module.exports = { ui: true }
